CDI TCK - Libraries
===================

Run the build to copy all CDI TCK dependencies into /target/dependency dir.

Specify the version of CDI TCK explicitly.

> mvn clean package -Dcdi.tck.version=4.0.1