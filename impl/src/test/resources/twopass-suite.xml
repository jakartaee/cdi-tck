<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >

<!-- Suite file for testing the two pass mode of running tests needed for annotation processor
implementations that rely on the SourceProcessor SPI.
-->
<suite name="TwoPass Example TestSuite" verbose="1" >
    <listeners>
        <!-- Optional - intended for debug purpose only -->
        <listener class-name="org.jboss.cdi.tck.impl.testng.ConfigurationLoggingListener"/>
        <listener class-name="org.jboss.cdi.tck.impl.testng.ProgressLoggingTestListener"/>
        <!-- Optional - it's recommended to disable the default JUnit XML reporter -->
        <listener class-name="org.testng.reporters.SuiteHTMLReporter"/>
        <listener class-name="org.testng.reporters.FailedReporter"/>
        <listener class-name="org.testng.reporters.XMLReporter"/>
        <listener class-name="org.testng.reporters.EmailableReporter"/>
        <listener class-name="org.testng.reporters.TestHTMLReporter" />
    </listeners>
    <test name="SourceProcessorTests" >
        <packages>
            <package name="org.jboss.cdi.tck.test.spi.*"/>
        </packages>
        <classes>
            <!-- The TestNG BeforeSuite implementation that tracks the run pass -->
            <class name="org.jboss.cdi.tck.impl.testng.SourceProcessorSuite" />
        </classes>
    </test>
</suite>